name: magnetnode_mobile
description: MagnetNode - Mobile app for managing qBittorrent downloads and magnet ingestion via Tailscale.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  provider: ^6.0.0
  intl: ^0.17.0

  flutter_local_notifications: ^17.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true





